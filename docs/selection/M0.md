# M0 baseline — 2026-09-16

Starting commit: e2fd95ad335d6b8c5967fd611c13a7b2a6b7b777.
Intentional existing changes preserved: .gitignore, README.md, pyproject.toml,
LINUX_AGENT_BRIEF.md, scripts/.

Commands (repository root, repo-local .venv):

```
git status --short
git rev-parse HEAD
nvidia-smi
.venv/bin/python -c 'import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name())'
rs-enumerate-devices -s
.venv/bin/pytest -q tests/test_object_perception.py tests/test_tabletop_box.py tests/test_box_estimator.py tests/test_pointcloud.py tests/test_spatial.py
.venv/bin/pytest -q
.venv/bin/ruff check .
.venv/bin/mypy
.venv/bin/python -m vision_pipeline.apps.tabletop_estimator --no-publish --max-frames 300
.venv/bin/python -m vision_pipeline.apps.record_rgbd recordings/d435i_m0 --frames 180
```

RTX 3070, 8192 MiB; driver 570.211.01, driver CUDA 12.8; CUDA available.
D435I 146322071961, firmware 5.17.3.10, USB 3.2, color BGR8 and depth Z16
both negotiated 640x480@30. Depth scale 0.0010000000474974513 m/unit.

Focused tests: 30 passed. Full: 129 passed, one inherited failure
`test_depth_lifter_rejects_discontinuous_and_missing_depth`.
Ruff: two inherited realsense.py findings B008/SIM105.
Mypy: 12 inherited errors in shm_seqlock.py and box_view_channel.py.

Geometric estimator: 300 submitted, 0 failed, steady windows 22.3–22.8 Hz;
first window 5 Hz includes startup. Raw output: m0-tabletop.log.
This estimator does not count sequence gaps or measure latency.

Recording: 180 lossless aligned pairs; receive rate 29.979 Hz,
wall rate including disk 29.960 Hz, zero color sequence gaps.
Read duration p50/p95 29.724/30.185 ms is **not capture latency**.
Original device/global-time and host-monotonic clock domains are preserved;
no cross-domain timestamp subtraction is used. Full device metadata and
intrinsics are in m0-capture.json and the recording.

Scene is dark equipment with no clearly staged tabletop test object. Existing
workspace bounds remain explicitly uncalibrated. No identity/quality acceptance
claim follows from the geometric estimator returning bounds.

## Addendum — takeover session, 2026-09-16 17:24–18:40 EDT

Commands (repository root, `.venv` active):

```
lsusb -t; rs-enumerate-devices -s
pytest -q
python -m vision_pipeline.apps.record_rgbd recordings/d435i_table_static --frames 300
```

- At takeover the D435I was absent from USB. After it was reconnected it enumerated on
  the **USB 2 root hub** (`lsusb -t`: Bus 01, port 3, `480M`; `usb_type_descriptor=2.1`),
  not the USB 3.2 link used for the numbers above. The stream still negotiated
  640x480@30 for both streams.
- USB 2.1 read-only loop (no disk, 10 s, `RealSenseSource.read()` incl. alignment):
  29.48 Hz, 6 color sequence gaps; SDK global-time capture->host p5/p50/p95/max
  12.4/43.0/78.0/79.9 ms. The earlier USB 3.2 hardware probe measured 33.8 ms mean.
- Full suite reproduced: 129 passed, 1 inherited failure (same test as above).
- New lit tabletop recording `recordings/d435i_table_static` (taped box, white ball,
  black remote, gripper, box on a side table): 300 frames, 29.78 Hz receive rate,
  2 color sequence gaps, capture->host p50/p95 41.3/71.5 ms while also writing to disk
  (USB 2.1). Diagnostics: `m0-table-static-capture.json`.
- The recording format is unchanged (`aligned-rgbd-v1`, so `d435i_m0` still replays);
  new recordings additionally store the negotiated device information in
  `manifest.json`, and replay verifies SHA-256 before decoding each frame.
